(function () {
    angular
        .module('eAspayb')
        .controller('EaspaybControlador', EaspaybControlador);

    EaspaybControlador.$inject = ['$log'];

    /* @ngInject */
    function EaspaybControlador($log) {
        /* jshint validthis: true */
        var eAspaybCtrl = this;
        
    }
})();
